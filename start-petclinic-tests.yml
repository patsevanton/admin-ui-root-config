version: '3'

services:
  start-petclinic-tests:
    image: drill4j/petclinic-autotests-execute:0.1.0
    environment:
      - petclinicUrl=http://host.docker.internal:8087
      - adminHost=host.docker.internal
      - adminPort=9090
      - agentId=dev-pet-standalone
